// split()는 string을 전달된 인자로 끊어 배열을 반환한다.

const fruits = 'apple-banana-orange';
const result = fruits.split('-');

// 전달한 인자 기준으로, 배열을 만들어 반환한다. 
// [ 'apple', 'banana', 'orange' ]
console.log(result);